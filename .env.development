# ─── DEVELOPMENT (demo mode) ──────────────────────────────────────────────────
# Caricato automaticamente da Vite in modalità "development" (npm run dev)
# Non serve un Supabase reale: l'app usa i dati demo locali.

VITE_DEMO_MODE=true

# Questi valori placeholder esistono solo per evitare errori TypeScript.
# In demo mode NON vengono mai usati per chiamate reali.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-key
